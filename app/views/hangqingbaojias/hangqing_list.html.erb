<div class="weizhi">当前位置：<%= link_to "首页", home_path %> > <%= link_to "行情报价", hangqingbaojias_path %>  > <font color="#FF6600"><%= @resource.name %>行情综述</font></div>
<div class="fl1_bottom">
  <div class="fl1_bottom_left">
    <div class="lby_left_top"><%= @resource.name %>行情综述</div>
    <div class="fl4_left_xiak">
      <ul>
        <% @hangqings.each_with_index do |hangqing, index| %>
          <li class="fl1_left_xiak_text">
          <%= link_to truncate(hangqing.title, length: 20), hangqing %>
          <font color="#999999"><%= hangqing.published_at %></font>
          </li>
          <% if index > 0 && index % 15 == 0 %>
            <div class="xuxian"></div>
          <% end %>
        <% end %>
      </ul>
      <%= will_paginate @hangqings %>
    </div>
  </div>
  <%= render partial: "right", locals: {resource: @resource} %>
</div>
