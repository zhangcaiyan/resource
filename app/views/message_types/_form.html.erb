<%= simple_form_for(@message_type, html: {class: "validate_form"}) do |f| %>

  <div class="inputs">
    <%= f.input :name, input_html: {class: "validate[required]"} %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
